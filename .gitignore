# Build artifacts (anchored to repo root so cmd/scc/ sources stay tracked)
/scc
/scc.exe
/scc-linux-*

# Go
*.test
*.out
vendor/

# Test matrix: compiled on demand by tests/binaries/build_binaries.sh
tests/binaries/output/

# Local fixture binaries (regenerable; the tracked matrix lives in tests/)
testdata/
